## Benchmarks for kafka REST API front

to start kafka server:

`docker compose up`

to compile and run rust:

`cargo build --release && ./target/target/rest_api`

to run nodejs:

`npm install express `

`npm install kafka-node``

`node server.js`
